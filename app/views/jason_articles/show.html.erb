<h2><%= @article.title %></h2>

<p>
  <%= @article.content %>
</p>

<p>&nbsp;</p>

<p>
  <b>Category</b>: 
  <%= @article.category.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', jason_articles_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_jason_article_path(@article), :class => 'btn' %>
  <%= link_to 'Delete', jason_article_path(@article), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
